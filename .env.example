# ===================================================
# SISTEMA DE EVALUACIÓN NUTRICIONAL INFANTIL
# Archivo de configuración de variables de entorno
# ===================================================
# 
# INSTRUCCIONES:
# 1. Copiar este archivo: cp .env.example .env
# 2. Editar .env con tus valores personalizados
# 3. NUNCA subir .env a git (ya está en .gitignore)
#

# ============ BASE DE DATOS ============
DATABASE_URL=postgresql://postgres:nutricion2024@db:5432/nutricion_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=nutricion2024
POSTGRES_DB=nutricion_db

# ============ BACKEND (FastAPI) ============
SECRET_KEY=tu_clave_secreta_super_segura_cambiala_en_produccion_123456789
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
BACKEND_BASE=http://backend:8000

# ============ FRONTEND (Next.js) ============
NEXT_PUBLIC_API_BASE=http://localhost:8000
NODE_ENV=development

# ============ REDIS (Tareas Asíncronas) ============
REDIS_URL=redis://redis:6379/0

# ============ DETECCIÓN DE ANEMIA ============
ANEMIA_ASYNC=true

# ============ DESARROLLO ============
PYTHON_ENV=development

# ===================================================
# NOTAS:
# - En producción, cambiar todas las contraseñas
# - Usar SECRET_KEY generado con: openssl rand -hex 32
# - Configurar CORS apropiadamente en producción
# ===================================================
